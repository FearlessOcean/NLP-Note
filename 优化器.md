# 只能用Adam优化器吗？这个是必须的嘛？
# 不是，**完全不是必须**。

`Adam` 只是一个常用优化器，不是唯一选择。你这句：

```python
optimizer = torch.optim.Adam(model.parameters(), lr=config.LEARNING_RATE)
```

意思只是：

**当前你选择用 Adam 来更新参数。**

---

## 常见优化器有哪些

### 1. SGD

最经典：

```python
optimizer = torch.optim.SGD(model.parameters(), lr=0.01)
```

也常配合动量：

```python
optimizer = torch.optim.SGD(model.parameters(), lr=0.01, momentum=0.9)
```

---

### 2. Adam

你现在用的这个：

```python
optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
```

特点一般是：

- 收敛往往比较快
    
- 对初学者友好
    
- 默认效果通常比较稳
    

---

### 3. AdamW

现在也很常见：

```python
optimizer = torch.optim.AdamW(model.parameters(), lr=0.001)
```

很多 Transformer、预训练模型里更常见。

---

## 优化器的作用是什么

不管你选哪种优化器，核心任务都一样：

**根据梯度去更新参数。**

也就是在 `loss.backward()` 之后，执行：

```python
optimizer.step()
```

只是不同优化器，**更新参数的规则不同**。

---

## 为什么很多教程喜欢先用 Adam

因为它通常：

- 不太挑初始学习率
    
- 训练前期比较省心
    
- 比纯 SGD 更容易看到 loss 下降
    

所以入门时常先用 Adam。

---

## 是不是必须用 Adam

不是。

你可以直接换，比如：

```python
optimizer = torch.optim.SGD(model.parameters(), lr=0.1)
```

代码照样能跑。

只是训练表现可能不同，比如：

- loss 下降速度
    
- 是否稳定
    
- 最终效果
    

都会受优化器影响。

---

## 你现在这个任务用 Adam 合不合理

合理，挺常见的。  
对于你这种：

- Embedding
    
- RNN
    
- Linear
    

用 Adam 完全正常，入门阶段也比较省心。

---

## 一句话

**Adam 不是必须的，优化器可以换；必须的不是 Adam，而是“要有一个优化器来根据梯度更新参数”。**

你下一句可以直接问我：**Adam 和 SGD 到底差在哪？**